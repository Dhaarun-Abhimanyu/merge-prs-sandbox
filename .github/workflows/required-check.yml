name: required-check
on: [pull_request]
jobs:
  required:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "required check intentionally failing for BLOCKED retest [DEFAULT-91]"
          exit 1
