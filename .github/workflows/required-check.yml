name: required-check
on: [pull_request]
jobs:
  required:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "required check intentionally failing for BLOCKED test [DEFAULT-89]"
          exit 1
